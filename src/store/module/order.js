import order from '../../api/order'
import * as types from '../mutation-types'

// initital state
const state = {
  quantity: 1,
}

// getters
const getters = {
  quantity: res => res.quantity,
}

// actions
const actions = {

}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations,
}
